version: "3.3"

services:
  test-course:
    build:
      context: image
      dockerfile: Dockerfile
    container_name: test-cpp-course
    cap_add:
      - SYS_PTRACE
    stdin_open: true
    tty: true
    volumes:
      - <mount_path>:/workspace
    ports:
      - "127.0.0.1:2227:22"
